import { ImageRuler } from "./imageRuler";
import { copiable } from "./copyable";

// Initialize the application when DOM is loaded
document.addEventListener('DOMContentLoaded', () => {
    new ImageRuler();
});